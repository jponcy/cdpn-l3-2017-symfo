<!DOCTYPE html>
<html>
<head>
    <meta charset=utf-8 />
    <title>Liste des animaux</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <a href="{{ path('app_animal_new') }}">Ajouter nouveau</a>
        <h1>Liste des annimaux</h1>

        {% if entities|length %}
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Espèce</th>
                    <th>Name</th>
                    <th>Weight</th>
                    <th>Birthdate</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td>{{ entity.id }}</td>
                    <td>{{ entity.species.name|default('') }}</td>
                    <td>{{ entity.name }}</td>
                    <td>{{ entity.weight }}</td>
                    <td>{{ entity.birthdate|date }}</td>
                    <td>
                        <a class="btn btn-default" href="{{ path('app_animal_edit', {id: entity.id}) }}">Edit</a>
                        <form action="{{ path('app_animal_delete', {id: entity.id}) }}" method="POST">
                            <input class="btn btn-danger" type="submit" value="delete" text="delete" />
                        </form>
                    </td> <!-- Edit animal. -->
                </tr>
            {% endfor %}
            </tbody>
        </table>
        {% else %}
            La liste est vide.
        {% endif %}
    </div>
</body>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
</html>